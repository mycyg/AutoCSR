"""M4 — multi-agent report writing.

Public surface:
    writer_agent.write_section(...)  -> SectionDraft
    orchestrator.write_all(...)      -> ReportDraft
    harmonizer.harmonize(...)        -> ReportDraft
    store.save_draft / load_draft / load_report
"""
